# contributor : Caker <eggcaker@gmail.com>
# name: Frontend Memcache Class
# group: LvShiWang
# --
class Memcache${1:class_name}
{
    protected static $s_instance = NULL;

    public static function fetch()
    {
        if (self::$s_instance === NULL)
        {
            self::$s_instance = new Memcache$1();
        }

        return self::$s_instance;
    }

    protected function __construct() {}

    public function cache($${2:in_obj})
    {
        LSWMemcache::memcache()->set('${3:m_id}'.$$2['id'], $$2);
    }

    public function load($in_id)
    {
        LSWMemcache::memcache()->get('$3'.$in_id);
    }

    public function clear($$2)
    {
        LSWMemcache::memcache()->delete('$3'.$$2['id']);
    }

}
